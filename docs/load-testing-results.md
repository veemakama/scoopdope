# Load Test Results

## Status

The k6 suite is configured, but no valid run has been captured in this
repository yet. The development machine did not have k6 installed during setup.
This is intentionally recorded as unmeasured rather than represented by
invented baselines.

## Required run record

After running against staging, copy the k6 text summary into the section below
and fill in the environment details.

| Field | Value |
| --- | --- |
| Date/time (UTC) | Not run |
| Commit SHA | Not run |
| Environment | Not run |
| API URL | Not run |
| Database/Redis size | Not run |
| k6 version | Not run |
| Profile | Not run |
| Result | Not run |

### Summary

```text
Pending first approved staging run.
```

### Interpretation

Do not compare future runs to the old numbers in historical documentation.
Those values have no checked-in raw output or reproducible run metadata and are
not treated as baselines. The first approved staging run establishes the
baseline for subsequent regression comparisons.
